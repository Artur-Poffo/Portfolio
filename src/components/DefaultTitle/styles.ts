import styled from "styled-components";

export const Title = styled.h1`
  position: absolute;
  top: 10vh;
  left: 50px;
  font-size: 2.3rem;
  text-transform: uppercase;
  letter-spacing: 2px;
  color: var(--primary);
`